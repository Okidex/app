# 2025 Standard Next.js + Firebase + IDX Gitignore

# --- Dependencies ---
node_modules/
**/node_modules/
.pnp
.pnp.js

# --- Next.js ---
# Ignore build output and traces
/.next/
/out/
.next-trace
# Next.js 15+ cache
.next/cache/

# --- Firebase & Hosting ---
# Ignore local CLI state and temporary framework builds
.firebase/
.firebaserc
# Keep hosting configs tracked
!firebase.json
!apphosting.yaml
!storage.rules
!firestore.rules
!firestore.indexes.json

# --- Development Environments (Firebase Studio / Project IDX) ---
# MUST be tracked to sync Node 22 + Tailwind plugin setup
!.idx/
!.idx/dev.nix
# Ignore local environment overrides
.idx/dev.local.nix

# --- Local Environment Variables (Secrets) ---
# Strictly ignore all local secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# Ignore specific Firebase secret files
.env.production
.env.staging

# --- Logs ---
*.log
*-debug.log
firebase-debug.log
ui-debug.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Production Build Artifacts ---
/build
/dist

# --- Testing ---
/coverage
.nyc_output

# --- IDEs & Editors ---
# Keep shared VS Code settings but ignore local workspace state
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*.sublime-workspace

# --- OS Specific ---
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
desktop.ini

# --- Infrastructure ---
.vercel
.turbo
