# This file was auto-generated by the Firebase CLI
# github.com

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # FIX 1: Explicitly define where the code is checked out on the runner.
          path: app
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies and build
        # FIX 2: Run the npm commands from inside the new 'app' directory.
        working-directory: ./app
        run: npm ci && npm run build
        env:
          # UPDATED: Pointing to the common SERVICE_ACCOUNT_JSON secret
          SERVICE_ACCOUNT_JSON: '${{ secrets.SERVICE_ACCOUNT_JSON }}'
          # UPDATED: Pointing to the common SERVICE_ACCOUNT_JSON secret (used by genkit/firebase tooling)
          FIREBASE_SERVICE_ACCOUNT: '${{ secrets.SERVICE_ACCOUNT_JSON }}'
          NEXT_PUBLIC_FIREBASE_API_KEY: '${{ vars.NEXT_PUBLIC_FIREBASE_API_KEY }}'
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: '${{ vars.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}'
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: 'studio-8509111427-a45a7'
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: '${{ vars.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}'
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: '${{ vars.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}'
          NEXT_PUBLIC_FIREBASE_APP_ID: '${{ vars.NEXT_PUBLIC_FIREBASE_APP_ID }}'
          NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID: '${{ vars.NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID }}'
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          # UPDATED: Pointing the action's input to the common SERVICE_ACCOUNT_JSON secret
          firebaseServiceAccount: '${{ secrets.SERVICE_ACCOUNT_JSON }}'
          channelId: live
          projectId: studio-8509111427-a45a7
          # FIX 3: Specify the entry point as the 'app' subdirectory for the deploy action.
          entryPoint: "./app"
          # FIX 4: Use the correct input name 'force' (boolean), not 'args'.
          force: true
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
          # UPDATED: Pointing to the common SERVICE_ACCOUNT_JSON secret in the action's env block
          FIREBASE_SERVICE_ACCOUNT: '${{ secrets.SERVICE_ACCOUNT_JSON }}'
          NEXT_PUBLIC_FIREBASE_API_KEY: '${{ vars.NEXT_PUBLIC_FIREBASE_API_KEY }}'
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: '${{ vars.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}'
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: 'studio-8509111427-a45a7'
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: '${{ vars.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}'
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: '${{ vars.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}'
          NEXT_PUBLIC_FIREBASE_APP_ID: '${{ vars.NEXT_PUBLIC_FIREBASE_APP_ID }}'
          NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID: '${{ vars.NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID }}'

