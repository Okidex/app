# This file was auto-generated by the Firebase CLI
# github.com

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://studio-8509111427-a45a7.web.app
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Build the Next.js application (Hardcoded Config Test)
        run: |
          # The following variables are HARDCODED FOR TESTING PURPOSES
          export NEXT_PUBLIC_FIREBASE_API_KEY="AIzaSyCN6uhJT_aUSTj5psl9Ru5viR50M7oyNY8"
          export NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="studio-8509111427-a45a7.firebaseapp.com"
          export NEXT_PUBLIC_FIREBASE_PROJECT_ID="studio-8509111427-a45a7"
          export NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="studio-8509111427-a45a7.firebasestorage.app"
          export NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="866288675038"
          export NEXT_PUBLIC_FIREBASE_APP_ID="1:866288675038:web:faff2fb1b41f0e10f47a4f"
          export NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID="G-X0QN9FM1BN"
          
          # Run the build command in the same shell session to inherit the exports
          npm ci && npm run build
        env:
          # This remains a secret because it is highly sensitive
          FIREBASE_SERVICE_ACCOUNT: '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}'
          FIREBASE_CLI_EXPERIMENTS: webframeworks
          # CI: false

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}'
          channelId: live
          projectId: studio-8509111427-a45a7
          entryPoint: .
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
